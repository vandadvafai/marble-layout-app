shapely>=2.0
pydantic>=2.0
numpy>=1.24
typer>=0.9
pytest>=7.0
matplotlib>=3.7
ezdxf>=1.0
streamlit>=1.30
reportlab>=4.0
# Used by tests/test_pdf_report.py to verify PDF content; not imported
# by runtime code.
pypdf>=5.0
# V1 ERP slab ingestion bridge (placement_engine/slab_intake/) — reads
# the messy Excel export and writes clean CSV/JSON for the engine.
pandas>=2.0
openpyxl>=3.1
# V1 image intake (placement_engine/image_intake/) — detects the green
# usable-area rectangle in slab photos and crops it. opencv-python is
# the headless-friendly binary distribution.
opencv-python>=4.8
